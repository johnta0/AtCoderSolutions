#include <iostream>
#include <vector>
#include <algorithm>
#include <string>

using namespace std;

/*
    D - Knapsack 1 https://atcoder.jp/contests/dp/tasks/dp_d
 */


int main() {
    int N, W;
    cin >> N >> W;

    vector<int> w(N), v(N);
    for (int i = 0; i < N; ++i)
    {
        cin >> w[i] >> v[i];
    }

    
}