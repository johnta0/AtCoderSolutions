#include <bits/stdc++.h>
#define rep(i, n) for(int i = 0; i < (n); ++i)
using namespace std;
using ll = long long;
using P = pair<int, int>;

int main() {
    int N;
    cin >> N;
    int A[N];
    rep(i, N) {
        if (i == 0)
            continue;
        cin >> A[i];
    }

    ans_list int[N];
    rep(i, N) {
        if (i == 0)
            continue;
    }

    return 0;
}
