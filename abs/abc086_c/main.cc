#include <iostream>
#include <vector>
#include <algorithm>
#include <string>

using namespace std;

int main() {
    int N;
    cin >> N;

    vector<int> t(N), x(N), y(N);
    for (int i = 0; i < N; ++i)
        cin >> t[i] >> x[i] >> y[i];

    vector<int> pos = {0, 0};
    
}